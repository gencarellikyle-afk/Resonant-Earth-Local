# RESONANT EARTH — CLAUDE STANDING INSTRUCTIONS
# Read this file every single session before touching anything.

---

## ⚠️ LEGAL REMINDER — EVERY SESSION WITHOUT EXCEPTION
Call Davenport Evans: **(605) 336-2880** — South Dakota Perpetual Purpose Trust.
Kyle personally owns everything right now. No LLC. No Trust. No bank account.
This must be resolved before anything scales. Cost: $3,000–$8,000.

---

## WHO WE ARE AND WHAT WE ARE BUILDING

Kyle Gencarelli is building **Resonant Earth** — civilizational infrastructure.
A parallel economic operating system designed to eliminate poverty, fund human
creativity, and reduce resource-based conflict through a self-sustaining grant
pool powered by options trading and voluntary contributions.

This is not a startup. This is not a side project. This is the most important
thing being built and every session must reflect that.

**Immutable mission voice (never change, never paraphrase):**
> "A shared system where individual presence creates collective results — on your own path."

**Live URL:** resonant-earth-local.vercel.app
**GitHub:** gencarellikyle-afk/Resonant-Earth-Local (branch: main)
**Local:** C:\Resonant-Earth-Local

---

## STACK — LOCKED PERMANENTLY

- Next.js 15.5.12, TypeScript, Tailwind, pure canvas 2D
- Supabase, Vercel, Stripe, Anthropic API
- **NO Three.js. NO @react-three/fiber. Ever. Zero exceptions. Permanent hard constraint.**
- Stripe: https://buy.stripe.com/5kQ9AUbeW9iA2oz21vffy00
- Supabase: qiztshrlsdjkozgyuotk.supabase.co

---

## PALETTE

The live `C` constant in the actual code files is the **single source of truth**
for current palette values. Always read the files first to confirm exact current
values before any visual work. Never assume palette values from memory.

**Visual philosophy (locked):**
- Heart is the absolute focal point and emotional center
- Globe is atmosphere only (opacity ~0.45)
- Strictly warm amber/gold/ember — never cool, clinical, or corporate
- Cursor is crosshair everywhere
- Visuals serve presence, not decoration

---

## ASSEMBLY PATTERN — NEVER CHANGES

- Parts: `page_a.txt` through `page_f2.txt`
- Assembly script: `assemble_page.mjs` assembles `["a","b","c","d","e","f1","f2"]` into `app/page.tsx`
- FieldNav written separately to `components/FieldNav.tsx`
- Every build: assemble first → `npm run build` → git if clean

---

## OPERATING RULES — NON-NEGOTIABLE

**Rule 1 — Read before touching anything.**
Before writing one line of code, read every file you plan to touch.
Not from memory. Not from previous sessions. Read the actual current files.
The live code is always the ground truth.

**Rule 2 — Surgical precision only.**
Identify exactly what is wrong. Change exactly those things.
Do not rewrite files that don't need rewriting.

**Rule 3 — Terminal-only flow.**
Every code change is delivered as exact PowerShell commands Kyle can paste
and run directly in his VS Code integrated terminal at C:\Resonant-Earth-Local.
Use `Set-Content` for full rewrites. Use `.Replace()` method for surgical
changes containing special characters.

**Rule 4 — One command at a time, in order.**
Commands are delivered sequentially. Each command is on its own clearly
labeled block. Never stack dependent commands without explicit instruction.

**Rule 5 — Build output required before git.**
`npm run build` must pass cleanly before any git commands are given.
If build fails: read the exact error, identify the exact cause, fix it.
Never push broken code.

**Rule 6 — No guessing, no assuming, no remembering.**
If uncertain about the current state of a file, read it.
Never proceed based on what you think a file looks like.

**Rule 7 — Screenshots are ground truth for visual issues.**
When Kyle sends screenshots those are the current state of the site.
Analyze every visible element. Name every problem specifically.

**Rule 8 — Confirmation before building.**
When a build involves multiple files or complex changes, confirm the complete
list of changes before writing any code. Kyle confirms. Then build.

**Rule 9 — Every session output is complete.**
Give Kyle everything he needs to execute completely — exact commands, in order,
with exact expected outcomes. No filler, no affirmations, no padding.

**Rule 10 — Memory and consistency.**
Maintain perfect consistency with all previous decisions, locked constraints,
and mission voice. If anything in current files contradicts locked decisions,
flag it immediately before proceeding.

---

## TARGET VISUAL STATE

**Hero section:**
- Deep dark warm brown background — radial bloom centered behind heart
- Warm amber center fading to near-black at edges. NO cool tones anywhere.
- "Resonant" — large Georgia serif, font-weight 300, ~10.5rem, rich amber-gold, above heart
- Heart — pure canvas 2D, layered concentric cream-white dot outlines (7 layers),
  warm gold glow at center, container min(400px, 72vw)
- "Earth" — same large scale as Resonant, italic, deep ember orange-red, below heart
- BreathingRings — warm amber/gold, wide, slow, full viewport width
- ToroidalField — wire mesh completely invisible (baseA:0.0 on all layers),
  only ceremony/surge effects fire on signal transmission
- No dark column bands on left or right edges
- Side panels float as subtle overlays — no borders, no dark zones

---

## KNOWN VISUAL PROBLEMS (unresolved)

1. Dark column borders left and right
2. ToroidalField canvas opacity still rendering
3. Top dark overlay too aggressive
4. Heart container too small
5. BreathingRings opacity too faint
6. Duplicate "On your own path." tagline

---

## TIER 1 CHANGES — HIGHEST PRIORITY (none completed yet)

- Heart as centerpiece with Resonant above / Earth below
- Pure canvas 2D FractalHeart (no Three.js)
- Open-ended contributions with anchor language only (no fixed tiers)
- Real signals rotating in typewriter display
- Returning visitor welcome message
- New country detection message
- Signal draft memory in sessionStorage
- Character count gradient on signal form
- Cursor warmth near heart
- Heart glow elevated 60s post-transmission
- Signal whispers left panel (9 items)
- Countries listed in right panel
- Founding signal in footer
- ECO hidden dot bottom right
- Field error language
- Single bottom linear-gradient vignette
- DPR/retina scaling on ToroidalField
- Differentiated surge ceremonies (signal vs contribution color)
- Spine opacity capped at 0.70 (now 0.0 — zeroed)
- Page title updates with coherence state
- Scroll depth scales torus
- Pure t*0.22 rotY — no rotation reset on heart touch
- Nodes across all 4 rScale depth layers with prime phi offsets
- Cream-gold surface baseline rgba(248,225,145) on outermost torus layer

---

## TIER 2 CHANGES — HIGH PRIORITY DEFERRED

- Field remembers new signal count since last visit
- Grant pool as metaphor ("holds enough for 3 relief grants")
- Coherence sparkline 7-day trend
- Heart double exposure effect
- Background dawn pulse synced to torus ambient
- Node trail motion blur effect
- Scroll progress indicator right edge
- Nav coherence fill bar

---

## CREATIVE SPARK — VISUAL PRIORITIES

1. Post-signal field response: Claude API generates one reflective sentence mirroring
   the essence of what was transmitted. Display in gold italics below confirmation.
2. Heart pulse on new realtime signal: single soft gold ripple/scale pulse from heart
3. Coherence-responsive field brightness: at High/Peak Coherence, brighten heart glow
4. Signal counter flash animation: briefly flash gold and scale up 1.1x on increment
5. Cursor warmth near heart: subtle warm radial glow within 150px of heart center
6. First-visit welcome: "You are arriving for the first time." fades in above signal form
7. Signal form draft memory: save partial input in sessionStorage, restore on return
8. Scroll progress indicator: 1px gold line on right edge
9. Nav coherence bar: 1px line under nav fills based on coherence %
10. Node hover labels: country code on desktop when near a signal node

---

## HOW TO START EVERY SESSION

1. Read every file you plan to touch (C palette constant, page_a.txt–f2.txt,
   FractalHeart.tsx, FieldNav.tsx, app/page.tsx)
2. Confirm you have read the actual current code
3. Proceed only after reading

This project is strictly Visual & Frontend in this context.
Legal, token, backend logic, coherence formulas, moderation, Arweave — separate chats.
